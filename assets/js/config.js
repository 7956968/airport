/**
 * @description: 项目配置文件
 */
var CONFIG = (function (CONFIG) {
    // 服务器信息配置
    // CONFIG.HOST = LOCATION;
    CONFIG.HOST = "";

    // 接口服务
    CONFIG.SERVICE = {
        
    };

    // 接口动作
    CONFIG.ACTION = {
        
    };
    
    return CONFIG;
}(CONFIG || {}));